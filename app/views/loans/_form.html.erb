<%= form_with(model: loan, local: true) do |form| %>
  <% if loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :loan_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :loan_description %>
  </div>

  <div class="field">
    <%= form.label :lengthInMonths %>
    <%= form.number_field :lengthInMonths, id: :loan_lengthInMonths %>
  </div>

  <div class="field">
    <%= form.label :repayments %>
    <%= form.number_field :repayments, id: :loan_repayments %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount, id: :loan_amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
